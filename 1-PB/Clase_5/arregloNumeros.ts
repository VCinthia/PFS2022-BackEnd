import * as rls from "readline-sync";

let num: number [] = new Array (7);
num = [20, 14, 8, 0, 5, 19, 24];

console.log("La cantidad de elementos es: "+ num.length);
console.log("Los elementos son: ", num.toString())